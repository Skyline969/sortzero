#!/usr/bin/env pythonimport sys, os, os.path;if len(sys.argv) != 2:	print "Error: Expecting 1 parameter, " + str(len(sys.argv) - 1) + " given."else:	print "Sorting " + os.path.realpath(sys.argv[1])	for dir in [o for o in os.listdir(sys.argv[1])]:		if os.path.isdir(os.path.realpath(sys.argv[1] + "/" + dir)):			dir = os.path.realpath(sys.argv[1] + "/" + dir)			print "Now sorting " + dir + ", please wait...."			for file in [f for f in os.listdir(dir)]:				if not os.path.isdir(os.path.realpath(dir + "/" + file)):					if file[0].isdigit():						subdir = "#"					else:						subdir = file[0].lower()					if not os.path.isdir(dir + "/" + subdir):						os.makedirs(dir + "/" + subdir)					os.rename(os.path.realpath(dir + "/" + file), os.path.realpath(dir + "/" + subdir + "/" + file))